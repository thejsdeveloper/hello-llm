# Agent Rules

## Python version & syntax

This project runs on Python 3.14 (`.python-version`, `requires-python >= 3.14`).
Always use modern Python features available in 3.14 and never fall back to old syntax:

## Tooling

- Dependencies and the virtualenv are managed with `uv` — use `uv add` / `uv run`, not `pip`.
